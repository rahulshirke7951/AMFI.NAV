{
  "_description": "Rules for scheme grouping, exclusion, and variant selection. Python uses this only for scheme-level decisions.",

  "base_scheme_remove_terms": [
    "DIRECT PLAN",
    "REGULAR PLAN",
    "GROWTH",
    "IDCW",
    "OPTION"
  ],

  "_base_scheme_remove_terms_desc": "Terms removed from scheme names to derive base scheme for grouping variants.",

  "exclusion_rules": {
    "contains_any": [
      "IDCW"
    ]
  },

  "_exclusion_rules_desc": "Schemes containing any of these keywords are excluded outright.",

  "selection_rules": {
    "priority_ladder": [
      ["REGULAR PLAN", "GROWTH"],
      ["REGULAR PLAN"],
      ["DIRECT PLAN", "GROWTH"],
      ["DIRECT PLAN"],

      ["REGULAR", "GROWTH"],
      ["REGULAR"],
      ["DIRECT", "GROWTH"],
      ["DIRECT"]
    ]
  },

  "_selection_rules_desc": "Ordered preference list. First matching variant is selected when multiple variants exist."
}
